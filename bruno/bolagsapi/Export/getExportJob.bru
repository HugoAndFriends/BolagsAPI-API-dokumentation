meta {
  name: getExportJob
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/export/{{announcementId}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  forwardAuthorizationHeader: false
}
