meta {
  name: retainExportJob
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/v1/export/{{announcementId}}/retain
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  forwardAuthorizationHeader: false
}
