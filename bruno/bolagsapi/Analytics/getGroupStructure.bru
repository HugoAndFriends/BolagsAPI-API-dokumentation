meta {
  name: getGroupStructure
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/v1/company/{{orgnr}}/group
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  forwardAuthorizationHeader: false
}
