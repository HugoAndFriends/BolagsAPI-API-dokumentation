meta {
  name: getCompanyCases
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/v1/company/{{orgnr}}/cases
  body: none
  auth: bearer
}

params:query {
  @description('Only cases starting or ending on or after this date.')
  ~from_date: 
  @description('Only cases starting or ending on or before this date.')
  ~to_date: 
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  forwardAuthorizationHeader: false
}
