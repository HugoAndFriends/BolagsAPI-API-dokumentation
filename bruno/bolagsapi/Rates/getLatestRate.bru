meta {
  name: getLatestRate
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/v1/rates/latest/{{seriesId}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  forwardAuthorizationHeader: false
}
