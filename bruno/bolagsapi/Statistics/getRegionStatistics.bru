meta {
  name: getRegionStatistics
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/v1/regions/{{code}}/statistics
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  forwardAuthorizationHeader: false
}
