meta {
  name: getCompanyStatistics
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/v1/company/{{orgnr}}/statistics
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  forwardAuthorizationHeader: false
}
