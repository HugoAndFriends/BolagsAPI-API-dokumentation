meta {
  name: getCreditorCallStatus
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/v1/company/{{orgnr}}/creditor-call
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  forwardAuthorizationHeader: false
}
