meta {
  name: getMarketQuery
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/v1/market/queries/{{query_id}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  forwardAuthorizationHeader: false
}
