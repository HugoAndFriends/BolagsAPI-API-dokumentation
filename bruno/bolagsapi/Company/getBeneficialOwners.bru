meta {
  name: getBeneficialOwners
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/v1/company/{{orgnr}}/beneficial-owners
  body: none
  auth: bearer
}

params:query {
  @description('Optional historical lookup date (YYYY-MM-DD)')
  ~datum: 
  @description('Set true to bypass cache and fetch fresh data')
  ~fresh: 
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  forwardAuthorizationHeader: false
}
