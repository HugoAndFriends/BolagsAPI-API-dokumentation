meta {
  name: getPersons
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/v1/company/{{orgnr}}/persons
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  forwardAuthorizationHeader: false
}
